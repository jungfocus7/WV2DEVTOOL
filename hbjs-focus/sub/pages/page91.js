//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
const fn_clear = () => {
};

const fn_stop = () => {
};

const fn_init = (pd) => {
};


export default {
    fn_clear, fn_stop, fn_init
};
